      - name: Claude Code Review
        if: always()
        uses: claude-ai/claude-code-review@v1
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          sharp-issues: true
          comment-on-pr: false
